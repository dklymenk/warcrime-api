import { BasePropertyProps } from 'adminjs';
import ReportPhoto from './report-photo';

const ReportPhotoList = (props: BasePropertyProps) => {
  return <ReportPhoto {...props} />;
};

export default ReportPhotoList;
